/*
You can choose to define all your middleware functions here,
export them and then import them into your app.js and attach them there.
app.use(myMiddleWare()). you can also just define them in the app.js if you like as seen in lecture 10's lecture code example.
If you choose to write them in the app.js, you do not have to use this file.

You will need middleware for:
1. Global request logging middleware
2. GET /signin guard middleware
3. GET /register guard middleware
4. GET /member authentication middleware
5. GET /manager authorization middleware
6. GET /signout authentication middleware
*/
